# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'mingle' do |p|
  p.developer 'James Coglan', 'james@songkick.com'
  p.extra_deps = [['activerecord', '>= 2.2']]
  # self.rubyforge_name = 'minglex' # if different than 'mingle'
end

# vim: syntax=ruby
